const CANVAS_H = 800;
const CANVAS_W = 1400;
const numberOfPoints = 1025;

let amplitude = 20;
let firstPoint, lastPoint;

let points = [] // array of y values

let seed = 45

function setup() {
    createCanvas(CANVAS_W, CANVAS_H)
    radnomSeed(seed);
    firstPoint = (random() * CANVAS_H / 10) + CANVAS_H / 2;
    lastPoint = (random() * CANVAS_H / 10) + CANVAS_H / 2;
}

function draw() {

}

function initialisePoints() {
    points[0] = firstPoint;
    points[numberOfPoints] = lastPoint;
    points[numberOfPoints / 2] 
}